## Мои любимые Linux снипеты

##### Показать только директории

```bash
$ ls -d */
```

##### Показать ip-адрес на машине

```bash
$ ifconfig | grep "inet " | grep -Fv 127.0.0.1 | awk '{print $2}'
```
